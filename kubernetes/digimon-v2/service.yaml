apiVersion: v1
kind: Service
metadata:
  name: digimon-fleet-v2-service
  namespace: digimon
spec:
  selector:
    app: digimon-fleet-manager-v2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
